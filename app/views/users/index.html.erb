<div class="col-xs-14">
		<div class="center">
        <p>Are your friends not in this list? No worries, send them an an invite in join Foospots.me as well. <%= share_snippet %></p>
    </div>
    <div id="pins" class="transitions-enabled friends">
      <% @user.each do |user| %>
        <div class="box panel panel-default" id = "user_<%= user.id %>">
            <%= render :partial => 'users/user', :locals => {:user => user} %>
        </div>
        <% end %>
    </div>
 </div>




