<% if current_user && current_user != user %>
  	 <% if current_user.following?(user) %>
   	 		   <p><%= link_to user.name, user_path(user)%> is your friend. <p>       
    	<% else %>
    	Add <%= link_to user.name, user_follow_path(user), :method => :post, data: { disable_with: "Please wait..." }, :remote => true %>
  	<% end %>
<% end %>


