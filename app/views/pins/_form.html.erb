<%= bootstrap_form_for(@pin) do |p| %>
    <%= p.text_field :name %>
    <%= p.text_field :address %>
    <%= p.text_field :place %>
    <%= p.submit "Save", class: "btn btn-default" %>
    <br></br>
<% end %>
